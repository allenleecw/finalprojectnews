<%= form_with(model: favcompany, local: true) do |form| %>
  <% if favcompany.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favcompany.errors.count, "error") %> prohibited this favcompany from being saved:</h2>

      <ul>
      <% favcompany.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :favcompany_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
